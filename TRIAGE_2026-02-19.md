# react-vnc Triage (2026-02-19)

Repo triaged: `roerohan/react-vnc`

## Pull Requests

### `#72` `feat(vncscreen.tsx): Allow for customization of onMouseEnter and onMouseLeave events`
- Status: Merge
- Why:
  - GitHub status is clean and mergeable.
  - Locally applies on current `main`.
  - Passes `npm run lint` and `npm run build:lib`.
  - Directly addresses `#5` (spurious `mouseleave` focus loss).
- Suggested maintainer comment:

```md
Thanks for this. I validated it on current `main` and the changes build cleanly.
This solves the longstanding `mouseleave`/focus issue tracked in #5, so I’m merging.
```

### `#62` `feat(vncscreen component): added missing handling for serververification event`
- Status: Do not merge as-is; replace with updated PR
- Why:
  - Conflicting with `main`.
  - Has unresolved requested changes.
  - Auto-approves server verification by default in the current implementation, which is not a safe default.
- Suggested maintainer comment:

```md
Thanks for the contribution. I’m closing this in favor of a refreshed PR because this branch is conflicting/stale and there are unresolved review requests.
We still want `serververification` support, but with a manual verification default (no silent auto-approve) and explicit API for approve/reject handling.
```

## Issues

### Keep Open

#### `#5` MouseLeave event triggered when clicking on the canvas
- Keep open until `#72` merges.
- Suggested comment:

```md
Still relevant and reproducible in Chrome-family behavior around noVNC mouse capture.
`#72` addresses this with both a safer default and overridable handlers. Keeping this open until merge.
```

#### `#56` Error "Tried changing state of a disconnected RFB object" on automatic reconnection
- Keep open.
- Suggested comment:

```md
Still relevant. Current reconnect flow can call `disconnect()` on an already disconnected session during retry transitions.
Needs a small state-handling fix and regression test before closing.
```

### Close as Resolved / Stale

#### `#42` Accept rawChannel or url for VncScreen component
- Close as resolved by websocket support (`v3.2.0`).
- Suggested comment:

```md
Closing as resolved. `VncScreen` now supports passing a pre-authenticated `websocket` directly (released in `v3.2.0`).
```

#### `#69` Cannot connect using react-vnc, but works with noVNC directly
- Close as not-a-bug (usage confusion), after docs clarification.
- Suggested comment:

```md
Closing as resolved/usage. The follow-up indicates the auth issue was due to prop shape confusion (`rfbOptions.credentials` nesting).
I’m also updating docs to make the credentials shape explicit.
```

### Needs Reproduction (close if no reporter feedback)

#### `#70` remove top & left black border
- Suggested comment:

```md
Need a minimal repro and CSS/container details to separate stream artifact vs component styling.
If no reproducible case is provided, we should close this as stale.
```

#### `#60` Failed while connected: Unexpected server message (type 154)
- Suggested comment:

```md
Need reproducible server details and a noVNC direct comparison on the same endpoint.
Without a repro or protocol trace we can’t confirm this is in `react-vnc` rather than server/noVNC compatibility.
```

#### `#44` scaleViewport screen becomes empty after tab changes
- Suggested comment:

```md
This is old and currently unreproduced. Please share a fresh repro against latest (`3.2.0`) with browser/version and layout details.
If there’s no follow-up, we should close as stale.
```

