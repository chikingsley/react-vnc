services:
  vnc:
    build:
      context: .
      dockerfile: tests/vnc-server/Dockerfile
    ports:
      - "6181:5900"
      - "6180:6080"
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 5900 && nc -z localhost 6080"]
      interval: 2s
      timeout: 1s
      retries: 30
